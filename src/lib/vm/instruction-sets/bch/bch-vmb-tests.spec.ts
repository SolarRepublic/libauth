import test from 'ava';

// const solution = 'New tests  Run "yarn doc:generate-tests" to correct this issue. (Note: watch tasks don\'t always update cached JSON imports when the source file changes. You may need to restart tsc.)';
test.todo('bch_vmb_tests.json is up to date');

// const solution = 'Running "yarn doc:generate-tests" will randomly generate UUID values for tests without a UUID. To correct this issue, copy those UUIDs back into the "bch_vmb_tests.ts" source (such that "yarn doc:generate-bch-vmb-tests" produces the same result each run).';
test.todo('bch_vmb_tests.ts has a unique, permanent UUID for every test');
